enum ConnectivityStatus {
  wiFi,
  mobileData,
  offline,
}

enum ThemeType {
  light,
  dark,
}

enum Gender {
  male,
  female,
}

enum MaritalStatus {
  single,
  married,
  widowed,
  divorced,
  separated,
}

enum CardType { card, date, cvc }
enum PaymentAppointmentTypes { hhc, phy, pcr, tele, hvd }

enum ChooseDateType { pcr, other, doctor, hide ,sleepMedicine,caregiver}
enum InstantConsultationStatus {
  send,
  timer,
  resend,
  approved,
  startMeeting,
  load,
  rating
}
